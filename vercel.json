{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"stage\" ]; then exit 1; else echo 'Skipping Vercel build on non-main/stage branch'; exit 0; fi"
}
